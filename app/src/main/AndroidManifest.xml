<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srilankantravellers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".second2">


        </activity>
        <activity android:name=".Main3Activity" >

        </activity>
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".mainDashboard" />
        <activity android:name=".resetpw" />
        <activity android:name=".view" /> <!-- <activity android:name=".dashboard" /> -->
        <activity android:name=".reg" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".Display" />
        <activity android:name=".Feedbacklist" />
        <activity android:name=".Feedback" />
        <activity android:name=".Review_update" />
        <activity android:name=".Showtour_review" />
        <activity android:name=".Llist" />
        <activity android:name=".form" />
        <activity android:name=".Activity2" />
        <activity android:name=".MainActivity2" >

        </activity>
        <activity android:name=".HotelsGalle"></activity>
        <activity android:name=".Hotels" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Showtour" />
        <activity android:name=".TourView" />
        <activity android:name=".MyTours" />
        <activity android:name=".ContactUs" />
        <activity android:name=".AboutUs" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>